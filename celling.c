#include <stdio.h>
#include <math.h>

int main() {
    float val;
    float fval, cval;
    
    printf("Enter the float value: ");
    scanf("%f", &val);
    
    fval = floor(val);
    cval = ceil(val);
    #include <stdio.h>
#include <math.h>

int main() {
    float val;
    float fval, cval;
    
    printf("Enter the float value: ");
    scanf("%f", &val);
    
    fval = floor(val);
    cval = ceil(val);
    
    printf("Floor value = %f\nCeiling value = %f\n", fval, cval);
    
    return 0;
}

    printf("Floor value = %f\nCeiling value = %f\n", fval, cval);
    
    return 0;
}
